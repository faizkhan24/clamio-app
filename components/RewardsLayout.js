// RewardsLayout.js
import React from 'react';
import { Outlet } from 'react-router-dom';

const RewardsLayout = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default RewardsLayout;
